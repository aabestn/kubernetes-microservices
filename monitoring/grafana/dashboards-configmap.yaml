apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-custom
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  cpu-throttling.json: |
    {
      "title": "Pod CPU Throttling Monitor"
    }
  latency-throughput.json: |
    {
      "title": "Traffic & Latency Overview"
    }